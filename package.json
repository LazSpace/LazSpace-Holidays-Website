{
  "name": "lazspace-holidays-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-sitemap.cjs && vite build",
    "preview": "vite preview",
    "sitemap": "node scripts/generate-sitemap.cjs"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  }
}
